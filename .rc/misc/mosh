# vim: set ft=sh: -*- mode: sh -*-

# Only for interactive shells.
[[ "${-}" != *i* ]] && return

[[ -x "$(command -v mosh)" ]] || return

export MOSH_PREDICTION_DISPLAY=never
export MOSH_TITLE_NOPREFIX=true
