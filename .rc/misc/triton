# vim: set ft=sh:

# Only for interactive shells.
[[ "${-}" != *i* ]] && return

# No need to do anything if the Triton CLI isn't installed.
[[ $(command -v triton) ]] || return


if [[ ! -f ${HOME}/.bash_completion.d/triton ]]; then
    triton completion > ${HOME}/.bash_completion.d/triton
fi

eval "$(triton env)"
