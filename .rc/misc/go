# vim: set ft=sh:

# Only for interactive shells.
[[ "${-}" != *i* ]] && return

# If Go is installed, let it set whatever variables it wants to.
[[ -x "$(command -v go)" ]] || return

# WTF WTF WTF WTF
export GOENV_DISABLE_GOPATH=1

export GOPATH="${HOME}/code/go"
append_to_path "${GOPATH}/bin"
