# vim: set ft=gitconfig ts=4 sw=4 et: -*- mode: conf-toml -*-

[commit]
    gpgsign = true

[user]
    name        = Seth Wright
    email       = seth@crosse.org
    signingkey  = ~/.ssh/id_ecdsa.pub
